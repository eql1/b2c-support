package com.equal.b2csupport;

import lombok.RequiredArgsConstructor;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
@RequiredArgsConstructor
public class TicketServiceTest {
//    private final TicketRepository ticketRepository;
//    private final TicketService ticketService;

    @Test
    // todo: realize unit testing for createTicket, read about mocking the authenticated user maybe?
    public void testSaveTicket() {
//        User mockUser = new User();
//        mockUser.
//
//        TicketRequest request = new TicketRequest("ticket", "ticket");
//
//        Ticket ticket = new Ticket(1, "ticket", "ticket", TicketStatus.OPEN, );
//        when(ticketRepository.save(any(Ticket.class))).thenReturn(ticket);
//
//        TicketResponse response = ticketService.createTicket(request);
//
//        assertNotNull(response);

    }
}
